# Leave empty in local development so browser requests use the Vite proxy.
VITE_API_BASE_URL=

# DigitalOcean App Platform sets the build-time literal `same-origin`.
# src/config/env.ts converts it to an empty browser base URL for /api requests.
